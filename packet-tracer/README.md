# Packet Tracer File

Add the working Cisco Packet Tracer topology here before publishing the repository.

Recommended filename:

```text
enterprise-network-security-lab.pkt
```

The generated repository does **not** include a `.pkt` file because the source provided for this conversion was the project report only.

Before committing the file:

1. Remove any student names, IDs, lecturer names, or university branding from Packet Tracer notes/labels.
2. Rename assignment-specific labels to portfolio-friendly names.
3. Confirm all devices are saved in a working state.
4. Run the checks in `../docs/validation.md`.
5. Take a clean full-topology screenshot and add it to `../assets/`.
